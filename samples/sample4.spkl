fn printAllChars(start, end){
	var x = 0;

	if(end <= start){
		ret 0;
	};

	printc(start);

	if(start == end){
		ret 0;
	};

	x = start + 1;
	ret printAllChars(x, end);
}

fn main(){
	printc('1');
	printc(':');

	var start = 'A';
	var end = 'z' + 1;

	printAllChars(start, end);
	newline();
	ret 0;
}
